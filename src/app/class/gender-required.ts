export class GenderRequired {
  gender: number;
  quantity: number;
  applied: number;
  constructor (gender, quantity, applied) {
    this.gender = gender;
    this.quantity = quantity;
    this.applied = applied;
  }
}
